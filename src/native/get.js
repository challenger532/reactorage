import { AsyncStorage } from "react-native";

export default data => {
  // data is an object that contains keys with values,,,,
  // if there is one key.. we can use get else we use multiGet
  // or we can use multiGet in all cases
};
