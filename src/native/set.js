import { AsyncStorage } from "react-native";

export default data => {
  // data is an object that contains keys with values,,,,
  // if there is one key.. we can use set else we use multiSet
  // or we can use multiSet in all cases
};
